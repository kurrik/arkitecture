platform {
  label: "Platform"

  gateway {
    label: "API Gateway"
  }

  services {
    label: "Services"

    users   { label: "Users" }
    orders  { label: "Orders" }
    billing { label: "Billing" }
  }
}

@layout {
  platform          { direction: vertical }
  platform.services { direction: horizontal }
}
